name: Our First Workflow

on : 
  push: 
    branches: ["main"]
  pull_request:


jobs:
  build_dotnet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses : actions/checkout@v3

      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '6.0.x'

      - name: 'dotnet build'
        run : dotnet build Next_Core_Blog.csproj

      - name: Print working directory
        run : pwd

      - name: Show list files
        run : ls -al

      - name: Show build files
        run : ls /home/runner/work/next_core_blog/next_core_blog/bin/Debug/net6.0/ -al

      - name: 'deploy file trasmit'
        uses: sebastianpopp/ftp-action@releases/v2
        with: 
          host: '110.8.21.243:26'
          user: 'pi'
          password: 'wlstncjs1!@#'
          localDir: '/home/runner/work/next_core_blog/next_core_blog/bin/Debug/net6.0/'
          remoteDir: '/home/pi/deploy'
          # port: ${{ secrets.PORT }}
         